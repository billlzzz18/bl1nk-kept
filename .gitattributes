# Default: normalize all text files to LF on checkout
* text=auto eol=lf

# Corpus files are checksum-verified upstream artifacts; git must never touch their bytes.
data/** -text

# Binary assets
*.zip binary

# GitHub Actions generated lock workflows
.github/workflows/*.lock.yml linguist-generated=true
